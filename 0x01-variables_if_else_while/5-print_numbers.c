#include <stdio.h>

/**
 * main - To show how to print all single digit numbers 10 from 0.
 *
 * Return: Always 0 (completed)
 */

int main(void)
{
	int i;

	for (i = 0; i < 10; i++)
		printf("%d", i);
	printf("\n");
	return (0);
}
